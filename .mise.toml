# Toolchain pin for Pjokk. `mise install` in the repo gives everyone the
# same node + pnpm without corepack (whose shim breaks on some setups).
# CI pins its own versions in .github/workflows/ci.yml — keep them in sync.
[tools]
node = "24"
pnpm = "11"
