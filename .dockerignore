# The image is COPY-only (see Dockerfile): everything is built natively by
# scripts/build-artifacts.sh first. So the build context is an allowlist —
# ignore the whole tree, then admit the two things the Dockerfile COPYs.
# This also keeps secrets (.env and friends) structurally out of reach.
*
!dist/server
!docker/data-skel
